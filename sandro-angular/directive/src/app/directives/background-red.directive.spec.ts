import { BackgroundRedDirective } from './background-red.directive';

describe('BackgroundRedDirective', () => {
  it('should create an instance', () => {
    const directive = new BackgroundRedDirective();
    expect(directive).toBeTruthy();
  });
});
