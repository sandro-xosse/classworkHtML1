import { DoublePipePipe } from './double-pipe.pipe';

describe('DoublePipePipe', () => {
  it('create an instance', () => {
    const pipe = new DoublePipePipe();
    expect(pipe).toBeTruthy();
  });
});
